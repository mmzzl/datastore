version: '3.8'
services:
  mongodb:
    image: mongo:7
    container_name: news-api-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  api:
    build: ../api
    container_name: news-api
    ports:
      - "8000:8000"
    volumes:
      - ../api/config.yaml:/app/config.yaml
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - mongodb
    restart: unless-stopped

  frontend:
    image: node:20-alpine
    container_name: news-frontend
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    depends_on:
      - api
    restart: unless-stopped

volumes:
  mongo_data:
