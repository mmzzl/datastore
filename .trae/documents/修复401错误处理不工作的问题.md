1. 修改 `responseInterceptor` 函数，让它同时检查 `res.statusCode` 和 `data.code` 是否等于 401
2. 确保在 HTTP 状态码为 401 时，无论响应体中的 `code` 字段是什么，都调用 `handle401Error` 函数
3. 测试修改后的代码，确保401错误时能正确跳转到登录页

修改点：

* 在 `responseInterceptor` 函数中，添加对 `res.statusCode` 的检查

* 当 `res.statusCode === 401` 或 `data.code === 401` 时，调用 `handle401Error` 函数

预期效果：

* 当服务器返回 401 状态码时，无论响应体格式如何，都会正确调用 `handle401Error` 函数

* 401错误处理流程会完整执行，包括清除用户状态和跳转到登录页

* 可以通过日志查看完整的处理流程

