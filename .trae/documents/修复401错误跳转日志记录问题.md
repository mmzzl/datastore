1. 修改 `uniCompat.js` 文件中的 `navigation` 对象，为 `redirectTo` 和 `switchTab` 方法添加 `success` 和 `fail` 回调函数支持
2. 确保在 H5 环境下，跳转方法也能正确调用回调函数，以便记录日志
3. 这样可以让 `handle401Error` 函数中的日志能够正确执行，帮助调试跳转问题

修改点：

* 在 `redirectTo` 方法中添加 `success` 回调调用

* 在 `switchTab` 方法中添加 `success` 回调调用

* 考虑添加错误处理逻辑，在跳转失败时调用 `fail` 回调

预期效果：

* 当401错误发生时，`handle401Error` 函数中的所有日志都会被正确记录

* 可以通过日志查看跳转流程的执行情况，定位跳转失败的具体原因

